apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "base-service-chart.fullname" . }}-test-connection"
  labels:
    {{- include "base-service-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "base-service-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
